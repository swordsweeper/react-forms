import React from "react";
import PropTypes from "prop-types";

export default function Input(props) {
    return (
        <div>
            <input type="text" />
        </div>
    );
}

Input.propTypes = {

};
